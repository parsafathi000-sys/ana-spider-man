# --- Web framework ---
fastapi>=0.115.0,<1
uvicorn[standard]>=0.30.0,<1
python-multipart>=0.0.9,<1
pydantic>=2.7.0,<3
pydantic-settings>=2.3.0,<3

# --- Database (async) ---
sqlalchemy>=2.0.30,<3
aiosqlite>=0.20.0,<1
asyncpg>=0.29.0,<1          # for PostgreSQL in production

# --- Auth / crypto ---
bcrypt>=4.1.0,<5
python-jose[cryptography]>=3.3.0,<4
cryptography>=42.0.0,<44

# --- QR code generation (SVG, no native deps) ---
qrcode>=7.4.2,<8

# --- System metrics (optional, graceful fallback) ---
psutil>=5.9.0,<7

# --- Testing ---
pytest>=8.2.0,<9
pytest-asyncio>=0.23.0,<1
httpx>=0.27.0,<1
aiosqlite>=0.20.0,<1
